{
  "name": "ahawin",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "AhaWin turns handwritten mistakes into misconception twins learners teach back.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check api/analyze.js && node --check api/health.js && node --check lib/core.js && node --check lib/rate-limit.js && node --check app.js",
    "test": "node --test",
    "eval": "node scripts/evaluate.mjs",
    "verify": "npm run check && npm test && npm run eval && bash scripts/secret-scan.sh"
  },
  "license": "MIT"
}
